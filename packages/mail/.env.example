# Hostinger SMTP (server-only — set in apps/web/.env.local for local dev)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=hello@vanderbase.com
SMTP_PASS=YOUR_SMTP_PASSWORD
SMTP_FROM="VanderBase <hello@vanderbase.com>"
